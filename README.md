# 猫格 MIBT

测试猫 MIBT 项目的 Next.js 起始工程。

## 本地开发

```bash
pnpm dev
```

浏览器打开 [http://localhost:3000](http://localhost:3000)。

首页入口位于 `src/app/page.tsx`，保存后会自动刷新。

## 可用命令

- `pnpm dev`：启动开发环境
- `pnpm build`：生成生产构建
- `pnpm start`：启动生产服务
- `pnpm lint`：运行代码检查
